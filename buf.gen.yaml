version: v1
plugins:
  # Our custom plugin (replaces standard go plugin)
  - plugin: go-values
    out: gen
    opt:
      - paths=source_relative
  # Generate gRPC services
  - plugin: go-grpc
    out: gen
    opt:
      - paths=source_relative